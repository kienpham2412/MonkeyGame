using UnityEngine;

public class SkillCard : MonoBehaviour
{
    // Handle what happens when player click on the skill card
    // When player click on this skill card, the monkey will correspond and perform the clicked skill
    // Send a message to the game orchestrator when it's clicked?
    // On receive the message, the game orchestrator will trigger the monkey to do something?
    private Skill _skill;
}